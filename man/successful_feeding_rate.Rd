% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/host_choice.R
\name{successful_feeding_rate}
\alias{successful_feeding_rate}
\title{Successful human blood-feeding rate}
\usage{
successful_feeding_rate(
  attempted_rate,
  indoor_destination,
  outdoor_destination,
  indoor_survival = 1,
  outdoor_survival = 1,
  indoor_feeding = 1,
  outdoor_efficiency = 1
)
}
\arguments{
\item{attempted_rate}{Attempted-feeding rate per mosquito per day,
\eqn{a^{*}}. Non-negative.}

\item{indoor_destination, outdoor_destination}{Post-repellency probabilities
that an attempt is indoor-human (\eqn{P^{in,R}}) or outdoor-human
(\eqn{P^{out,R}}).}

\item{indoor_survival, outdoor_survival}{Residual survival of an attempt
through the indoor (\eqn{K^{in}}) and outdoor (\eqn{K^{out}}) pathways, in
\eqn{[0, 1]}. Default 1 (no killing).}

\item{indoor_feeding}{Residual successful feeding given a survived indoor
attempt and the barrier, \eqn{B^{in}}, in \eqn{[0, 1]}. Default 1.}

\item{outdoor_efficiency}{Outdoor feeding efficiency \eqn{F^{out}} in
\eqn{[0, 1]}. Default 1.}
}
\value{
The successful human blood-feeding rate \eqn{a}, the shape of the
recycled inputs.
}
\description{
Assembles the realised, successful human blood-feeding rate \eqn{a} from the
attempted-feeding rate and the destination, killing, and barrier effects, in
the biological order attempt -> repellency/redistribution -> pre-feed killing
-> barrier -> successful feed (maths spec section 16):
\deqn{a = a^{*}\left[P^{in,R} K^{in} B^{in} + P^{out,R} K^{out} F^{out}\right].}
}
\details{
Only indoor- and outdoor-human attempts can produce a human blood meal;
animal-directed attempts never contribute (though they may still kill, see
\code{\link[=mortality_hazard]{mortality_hazard()}}). Destination probabilities come from
\code{\link[=host_destination]{host_destination()}}; killing and barrier terms come from an intervention
mapping such as \code{\link[=intervention_effect_stub]{intervention_effect_stub()}}.
}
\examples{
d <- host_destination(3, 1, 1, residual_accessibility = 0.6)
successful_feeding_rate(
  attempted_rate = 0.3,
  indoor_destination = d$indoor,
  outdoor_destination = d$outdoor,
  indoor_survival = 0.7,
  indoor_feeding = 0.6
)
}
\references{
\code{MATHEMATICAL_SPEC_WORKING.md} section 16.
}
