% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capacity.R
\name{vectorial_capacity}
\alias{vectorial_capacity}
\title{Species-specific vectorial capacity (Garrett-Jones form)}
\usage{
vectorial_capacity(
  infection_probability,
  abundance,
  feeding_rate,
  survival,
  eip
)
}
\arguments{
\item{infection_probability}{Probability \eqn{c} that a mosquito becomes
infected after biting an infectious human. In \eqn{[0, 1]}.}

\item{abundance}{Adult mosquitoes of the species per human, \eqn{m}.
Non-negative.}

\item{feeding_rate}{Realised, successful human blood-feeding rate per
mosquito per day, \eqn{a}. Non-negative.}

\item{survival}{Daily adult survival probability, \eqn{p}. Strictly between
0 and 1.}

\item{eip}{Extrinsic incubation period in days, \eqn{\nu}. Positive.}
}
\value{
A numeric vector (or array, following the recycled inputs) of
species-specific vectorial capacity, in the same units as \code{abundance}
(per human).
}
\description{
Computes the stationary Garrett-Jones vectorial capacity for a single
mosquito species, at each location and time. This is the core quantity of
the framework: the vector-side transmission potential attributable to one
species, before any summation across species (\code{\link[=sum_species]{sum_species()}} sums them).
}
\details{
The equation is
\deqn{V = c\, m\, a^{2}\, \frac{p^{\nu}}{-\log p},}
with symbols as in the arguments below. All arguments are vectorised and
recycled, so the function works elementwise over arrays of any shape (over
location, time, and draw); it is called once per species.

\code{survival} is the daily adult survival probability \eqn{p}. Where survival is
assembled from a mortality hazard \eqn{\mu} (for example under interventions,
see the maths spec), pass \eqn{p = \exp(-\mu)}; the two forms are identical.
}
\examples{
vectorial_capacity(
  infection_probability = 0.5,
  abundance = 12,
  feeding_rate = 0.3,
  survival = 0.9,
  eip = 11
)
}
\references{
\code{MATHEMATICAL_SPEC_WORKING.md} section 1 (and section 17.2 for the
equivalent mortality-hazard form). The maths spec is authoritative.
}
