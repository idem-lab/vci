% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{vci_by_scenario}
\alias{vci_by_scenario}
\title{Vector control impact of each scenario against a reference}
\usage{
vci_by_scenario(capacity, reference)
}
\arguments{
\item{capacity}{A data frame from \code{\link[=compute_capacity]{compute_capacity()}}, with columns
\code{location_id}, \code{scenario}, and \code{capacity}.}

\item{reference}{The scenario label to treat as the reference; must appear in
\code{capacity$scenario}.}
}
\value{
A tidy data frame with one row per district and scenario:
\code{location_id}, \code{scenario}, and \code{vci} (a proportion; multiply by 100 for a
percentage).
}
\description{
Turns the district-by-scenario capacities from \code{\link[=compute_capacity]{compute_capacity()}} into
vector control impact, comparing every scenario against a named reference
within each district: \eqn{VCI = 1 - V_\mathrm{alt}/V_\mathrm{ref}} (see
\code{\link[=compute_vci]{compute_vci()}}). The reference scenario itself yields a VCI of zero.
}
\examples{
capacity <- compute_capacity(rwanda_inputs)
vci_by_scenario(capacity, reference = "none")
}
