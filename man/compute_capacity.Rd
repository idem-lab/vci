% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{compute_capacity}
\alias{compute_capacity}
\title{Compute total vectorial capacity per district and scenario}
\usage{
compute_capacity(
  inputs,
  scenarios = c(none = "none", bau = "pyrethroid", alt = "dual_ai")
)
}
\arguments{
\item{inputs}{A \link{vci_inputs} object.}

\item{scenarios}{A named character vector mapping scenario label (the name)
to net product (the value), each of \code{"none"}, \code{"pyrethroid"}, or
\code{"dual_ai"}. Defaults to the three Milestone 0 scenarios: \code{none},
business-as-usual \code{bau} (pyrethroid), and the \code{alt} alternative (dual-AI).}
}
\value{
A tidy data frame with one row per district and scenario:
\code{location_id}, \code{scenario}, and total \code{capacity}.
}
\description{
Runs the full Milestone 0 pipeline over a \link{vci_inputs} object: for each
scenario it applies the intervention stub, assembles the successful-feeding
rate and mortality hazard, evaluates the species-specific capacity equation,
and sums across species to a district total. The intervention-independent
inputs (abundance, host weights, baseline hazard, EIP) are shared across
scenarios; only the net product and its coverage differ.
}
\details{
A scenario is one net product applied at the district's net use: \code{"none"}
deploys nothing (coverage 0), while a net product (\code{"pyrethroid"} or
\code{"dual_ai"}) is applied at the site \code{net_use}, scaled by each species'
in-bed contact fraction (\code{net_contact}). The mapping from product to effects
is a stub (\code{\link[=intervention_effect_stub]{intervention_effect_stub()}}); results are illustrative only.
}
\examples{
capacity <- compute_capacity(rwanda_inputs)
head(capacity)
}
\seealso{
\code{\link[=vci_by_scenario]{vci_by_scenario()}} to turn these capacities into VCI.
}
