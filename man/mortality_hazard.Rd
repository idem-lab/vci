% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/host_choice.R
\name{mortality_hazard}
\alias{mortality_hazard}
\title{Total adult mortality hazard under interventions}
\usage{
mortality_hazard(
  baseline_hazard,
  attempted_rate,
  indoor_destination,
  outdoor_destination,
  animal_destination,
  indoor_survival = 1,
  outdoor_survival = 1,
  animal_survival = 1
)
}
\arguments{
\item{baseline_hazard}{Baseline adult mortality hazard per day \eqn{\mu^{0}},
before intervention killing. Non-negative.}

\item{attempted_rate}{Attempted-feeding rate per mosquito per day
\eqn{a^{*}}. Non-negative.}

\item{indoor_destination, outdoor_destination, animal_destination}{Post-repellency destination probabilities \eqn{P^{e,R}} from
\code{\link[=host_destination]{host_destination()}}.}

\item{indoor_survival, outdoor_survival, animal_survival}{Residual survival of
an attempt through each pathway \eqn{K^{e}}, in \eqn{[0, 1]}. Default 1 (no
killing).}
}
\value{
The total adult mortality hazard \eqn{\mu} per day, the shape of the
recycled inputs.
}
\description{
Combines the baseline mortality hazard with intervention-mediated killing,
accumulated across all three feeding pathways as a stationary competing
hazard (maths spec section 17):
\deqn{\mu = \mu^{0} + a^{*}\sum_{e} P^{e,R}\left(1 - K^{e}\right).}
}
\details{
Killing on every pathway contributes, including the animal pathway (a
mosquito can be killed at an animal-directed attempt even though that attempt
yields no human blood meal). The barrier term \eqn{B} does not enter here — it
prevents feeding, not survival. Convert the hazard to a survival probability
with \code{\link[=survival_probability]{survival_probability()}}.
}
\examples{
d <- host_destination(3, 1, 1, residual_accessibility = 0.6)
mortality_hazard(
  baseline_hazard = 0.12,
  attempted_rate = 0.3,
  indoor_destination = d$indoor,
  outdoor_destination = d$outdoor,
  animal_destination = d$animal,
  indoor_survival = 0.7
)
}
\references{
\code{MATHEMATICAL_SPEC_WORKING.md} section 17.
}
