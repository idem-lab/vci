.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj
inst/doc
docs/
.claude/settings.local.json

# Fallback location for the session-log hook's per-session state, used only when
# the git dir cannot be located. Never commit it. (See .claude/hooks/log-session.sh.)
dev/sessions/.state/
